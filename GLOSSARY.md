# 用語集

各層の README から参照する用語をここにまとめる。security-lessons の GLOSSARY.md と内容が重なる用語（TLS / 証明書 / CA / MITM など）はそちらを参照。

---

## VM (Virtual Machine / 仮想マシン)

1 台の物理マシンの上で動く、独立した「もう 1 台のコンピュータ」。OS ごとまるごと隔離されている。

本プロジェクトでは、1 台の Mac の上に Linux サーバを複数台立てて、サーバ間通信を再現するために使う。

---

## Multipass

Canonical 製の VM ツール。**Ubuntu の VM をコマンド 1 発で立てられる**ことに特化している。

```bash
multipass launch --name node1 22.04   # Ubuntu 22.04 のVMを「node1」という名前で起動
multipass shell node1                 # node1 にSSHログイン
multipass list                        # 立てたVMの一覧
multipass stop node1                  # 停止
multipass delete node1                # 削除
```

GUI を持たない。すべてターミナルから操作する。

---

## パッケージ / パッケージマネージャ

「ソフトウェア 1 本」を、依存関係・設定ファイル込みでひとまとめにした配布形式。

| 系統 | パッケージ形式 | コマンド |
|---|---|---|
| RHEL 系 (RHEL, AlmaLinux, Rocky) | `.rpm` | `yum` / `dnf` |
| Debian 系 (Debian, Ubuntu) | `.deb` | `apt` |

**パッケージマネージャ**は、リポジトリからパッケージをダウンロードして、依存関係を解決しつつインストールするツール。

---

## リポジトリ (パッケージリポジトリ)

パッケージを **大量に並べた配信元** のこと。HTTP/HTTPS で公開されている「ファイル置き場 + メタデータ」。

クライアント側 (例: Ubuntu) の `/etc/apt/sources.list` に「このURLから取りに行く」と書いておくと、`apt install` 時にそこから取ってくる。

**RHUI を一言で言うと「自前のリポジトリを大規模・認証付きで運用する仕組み」**。

---

## GPG 署名 (パッケージ署名)

パッケージが「正しい発行元から来た、改ざんされていないもの」であることを暗号で証明する仕組み。

仕組みはサーバ証明書と同じ「公開鍵暗号 + 署名」。違いは:

- TLS は **通信** に署名する（伝送中の改ざん検知）
- GPG は **ファイルそのもの** に署名する（保存後の改ざん検知）

クライアントは事前に発行元の **公開鍵 (GPG key)** を持っていて、パッケージの署名と照合する。署名が合わなければ `dnf` / `apt` がインストールを拒否する。

---

## クライアント証明書認証 (mTLS / mutual TLS)

通常の HTTPS では **サーバが自分の証明書を出してクライアントに検証される**。これは「サーバが本物か」をクライアント側が確かめる仕組み。

**クライアント証明書認証** は逆方向も足す。**クライアントも証明書を出してサーバに検証される**。これで「**誰がアクセスしてきたか**」をサーバが識別できる。

RHUI では「契約者だけが配信を受けられる」状態を作るためにこれを使う。本演習でも同じ仕組みを自作 CA で再現する。

---

## ロードバランサ (LB) / 負荷分散

クライアントからのリクエストを、**複数のサーバに振り分ける**装置。

目的:

1. **負荷分散** — 1 台では捌けない量を複数台で受ける
2. **冗長化** — 1 台が落ちても残りで処理を続ける
3. **SSL終端** — TLS の暗号化/復号を LB でまとめて引き受け、後ろのサーバは平文で扱う

---

## HAProxy

代表的なオープンソースのロードバランサ。設定ファイル (`haproxy.cfg`) に「どこから受けて」「どこへ流すか」を書いて動かす。

L4 (TCP レベル) も L7 (HTTP レベル) も扱える。RHUI v5 にも標準で同梱されている。

---

## ヘルスチェック

LB が後ろのサーバに対して、**「お前は生きてるか」を定期的に問い合わせる**仕組み。

死んでいると判定されたサーバには振り分けを止める。よくあるのは「`/health` というURLに HTTP リクエストを投げて 200 が返るか」を見る方式。

---

## IaC (Infrastructure as Code)

サーバ構築の手順を **コードとして書いて再現可能にする** 考え方。

「サーバAに入って手で apt install して設定ファイル書いて…」を何台もやる代わりに、コードを書いておけば何台でも同じ状態を再現できる。

---

## Ansible / Playbook

代表的な IaC ツール。YAML で書いた手順 (`Playbook`) を、対象サーバ群 (`インベントリ`) に対して順番に実行する。

特徴:

- 対象サーバに **エージェントを入れなくていい**（SSH で十分）
- **冪等性** — 同じ Playbook を何度実行しても、最終状態は同じ（既に適用済みなら何もしない）

RHUI v5 のセットアップそのものも Ansible Playbook で提供されている。

---

## RHUI 関連用語

| 用語 | 意味 |
|---|---|
| RHUI | Red Hat Update Infrastructure。RHELサーバ群へのアップデートを自前で配信する仕組み |
| RHUA | Red Hat Update Appliance。上流(Red Hat CDN)からコンテンツを取得・管理する中枢ノード |
| CDS | Content Delivery Server。実際にクライアントへリポジトリを配信するノード（複数台でスケール） |
| エンタイトルメント証明書 | 「この契約者は配信を受ける権利がある」ことを示すクライアント証明書 |
| Pulp | RHUI の中で実際にリポジトリ管理を担っている OSS。v4 は Pulp 2 系、v5 は Pulp 3 系 |

---

## createrepo_c

`.rpm` ファイルが入ったディレクトリから、リポジトリとして必要なメタデータ (`repodata/`) を生成するツール。

Debian 系での対応は `apt-ftparchive` や `aptly` など別ツール。本演習 (Ubuntu) では後者を使う。
